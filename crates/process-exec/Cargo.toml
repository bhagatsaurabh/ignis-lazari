[package]
name = "process-exec"
version = "0.4.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["process", "time", "rt"] }
thiserror = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
